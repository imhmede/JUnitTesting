/**
 * This program is designed to recieve a word and return the number of vowels that the 
 * word has in it.
 * @author Jordan Arguello
 * Oct,31,23
 */
Scanner.in = new Scanner(System.in);
public class vowels {
    /**
     * This is the main method and will take a word from the user and tell you how many vowels 
     * it has in it.
     * @param args a list of all possible arguments for the program.
     */
    counter = 0
    public static void main(String [args]) {
        System.out.println("Please enter your word");
        Word = in.nextString();
        for (index = "a"; counter ++)
        for (index = "e"; counter ++)
        for (index = "i"; counter ++)
        for (index = "o"; counter ++)
        for (index = "u"; counter ++)
        for (index = "y"; counter ++)
        System.out.println("The number of vowels is " + counter)
    }

}