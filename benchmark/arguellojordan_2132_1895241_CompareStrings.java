
/** This program is designed to recieve two strings of characters and decide if they are the 
 * same or different.
 * @author Jordan Arguello
 * Oct,25,23
 */
import.java.util.Scanner;
public class CompareStrings{
    /** This method will compare the strings and determin if they are the exact same string 
     * or if they are different.
     * @param args a list of all possible arguments.
     */
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        System.out.println("Enter the first string:");
        FirstString = in.nextString();
        System.out.println("Enter the second string:");
        SecondString = in.nextString();
        if
        (FirstString == SecondString);
        System.out.println("All the same");
        else
        System.out.println("Neither");
    }
}