/******************************************************************************

                            Online Java Compiler.
                Code, Compile, Run and Debug java program online.
Write your code in this editor and press "Run" button to execute it.

*******************************************************************************/
//Kiari Roberts
//a code thatthat checks whether two arrays have the same elements in the same order. 
//In the program, the two arrays must be populate with random numbers
//4-24-23
public class Main
{
     public static void main(String[] args)
     {
public static boolean equals(int[] a, int[] b);
}

if(a.length!=b.length)
//Checks the Lentgh 
return false;

for(int i=0;i<a.length;i++)
{//check if same

if(a[i]!=b[i])return false;
println("The two arrays do not have the same elements in the same order")
//of it doesnt match the equation then the code will print "The two arrays do not have the same elements in the same order"

}

return true;
println("The two arrays do have the same elements in the same order")
//"if it does follow the equation it will print"The two arrays do have the same elements in the same order"
}
}
}